version: '3.7'
services: 
  nginx:
    build: ./conf
  webpack-dev:
    image: node:11.3.0
    command: npm run container
    working_dir: /root/project
    volumes: 
      - "./:/root/project"
    ports:
      - "8080:8080"
    extra_hosts: 
      - "postgrest:10.245.228.207"
