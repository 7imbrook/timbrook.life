apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-service
  namespace: {{.Values.environment.namespace}}
spec:
  ports:
  - port: 80
    protocol: TCP
    name: http
  selector:
    app: {{.Release.Name}}-app