# Main Site
localhost:8000 {
  proxy /api {
    upstream http://postgrest.postgrest.bbbad713.svc.dockerapp.io:9001
    without /api
    transparent
  }
  root ./dist
  rewrite {
    if {path} not_match \/api/.*
    if {file} not_match (.*\.js|.*\.html|.*\.css)
    to /index.html
  }
}
