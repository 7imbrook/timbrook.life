replicaCount: 1

image:
    repository: 7imbrook/podcast

healthEndpoint: "/pod.xml"

container:
    port: 4000

ingress:
    enabled: true
    hosts:
        - timbrook.tech 
    paths:
        - /pod.xml
        - /api/upload
        - /api/configure

secrets:
    - jwt-keys